import { fetchWeatherDataByCity } from "./geolocation.js";

// Intialize the app when the DOM is loaded
document.addEventListener("DOMContentLoaded", function () {
  // Add event listeners for weatherBtn
});
