const searchBar = document.getElementById ('searchBar')
const weatherDisplay  = document.getElementById ('weatherDisplay')
const  weekWeather = document.getElementById ('weekWeather')


// 1. Weather of the city in the starting page Complete Json

/* 1.1 Json filter by 
- // city weather now
- // icon of the now weather in the back
-// max and min temperature of the day 
-// feels like
-// sunrise time and sunset time


//2. wetaher of the week commpleate json 

//3. search new city compleate json


















/*const container = document.getElementById('astros')

fetch('http://api.open-notify.org/astros.json')
.then ((response) => {
    return response.json()
})
.then((json) => {
    container.innerHTML = `<h1>There are ${json.number} people 
    in space rigth now<h1>`

    json.people.forEach((person) => {
        container.innerHTML += `<p>${person.name} is 
        on the ${person.craft} </p>`
    })
})*/