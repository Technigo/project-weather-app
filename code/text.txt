Weather app to do

// divide the url up
// create hamburger menu
// when it is clicked, should forecast be shown?

// add icon to current weather

// add button to switch location
// if button is clicked, change town and fetch the data for that country







// Eller så här:

const cities = [
  { name: "Varberg", country: "Sweden" },
  { name: "Stockholm", country: "Sweden" },
  { name: "Oslo", country: "Norway" },
];

// const URL = `${BASE_URL}${cityName},${countryName}&units=metric&APPID=${API_KEY}`;

<div id="button-container"></div>

function createCityButton(cityName, countryName) {
  const container = document.getElementById("button-container");

  const button = document.createElement("button");
  button.textContent = `${cityName}, ${countryName}`;
  button.addEventListener("click", () => {
    fetchData(cityName, countryName);
  });

  container.appendChild(button);
}

createCityButton("Varberg", "Sweden");
createCityButton("Stockholm", "Sweden");
createCityButton("Oslo", "Norway");
// Lägg till fler städer här om du vill




// const URL = `${BASE_URL}${cityToUrl}&units=metric&APPID=${API_KEY}`
// //console.log(URL)

// const fetchWord = () => {
//   fetch(URL)
//     .then((response) => response.json())
//     .then((data) => {
//       console.log("hej")
//       console.log(data)
//     })
//     .catch((error) => console.log("Error ❌", error))
// }



